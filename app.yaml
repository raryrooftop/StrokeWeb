runtime: python312
instance_class: F2

entrypoint: uvicorn backend.main:app --host 0.0.0.0 --port $PORT

env_variables:
  PYTHONPATH: "."

# 핸들러 설정 (필요 시 정적 파일 최적화 가능)
handlers:
- url: /assets
  static_dir: frontend/dist/assets

- url: /.*
  script: auto
